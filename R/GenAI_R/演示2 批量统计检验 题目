

场景：
你希望探究结肠癌患者新辅助治疗不同终点(pCR, non-pCR)是否有不同的外周血白细胞模式，
故收集了50名pCR和50名非pCR患者的外周血，通过流式细胞术，获得了细胞比例，此时，你希望：
1. 将数据导入R并检查是否有缺失值
(数据文件名为：“pCR.csv”，就在当前目录下)
2. 计算每种细胞在不同outcome组的中位数（不用dplyr包，用for循环来做）
(数据框中总共有12列，第一列是患者ID，不用参与统计，第二列为"outcome"，有两个取值“pCR”和“nonpCR”,
第三到第12列为细胞种类，名字为“cell1”-“cell10”)
3. 检验每种细胞在不同终点组是否有差异（wilcoxon检验），并输出p值
4. 对P值进行校正（FDR）
5. 对于上一步计算出的results变量，筛选p.adjust<0.05的结果
对于上一步计算出的significant_results变量，将中位数也附上去
